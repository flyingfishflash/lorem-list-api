---

spring:
  config:
    activate:
      on-profile: "h2"
  datasource:
    username: "sa"
    url: "jdbc:h2:mem:lorem-list;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=TRUE;DATABASE_TO_LOWER=true;MODE=PostgreSQL"
    password: ""
  h2:
    console:
      settings:
        web-allow-others: "true"
    console.enabled: true
